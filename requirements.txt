# Core dependencies - cross-platform compatible
streamlit>=1.28.0
numpy>=1.24.0
scipy>=1.11.0
pandas>=2.0.0

# Geometry and visualization (Altair-first, cross-platform)
shapely>=2.0.0
trimesh>=4.0.0
altair>=5.0.0
svgwrite>=1.4.3
plotly>=5.17.0

# Image processing (cross-platform)
opencv-python>=4.8.0; platform_system != "Darwin" or platform_machine != "arm64"
opencv-python-headless>=4.8.0; platform_system == "Darwin" and platform_machine == "arm64"
scikit-image>=0.21.0
Pillow>=10.0.0

# Optimization (cross-platform)
scikit-optimize>=0.9.0
cma>=3.3.0

# Testing (cross-platform)
pytest>=7.4.0
pytest-cov>=4.1.0

# Optional AI/ML (install separately based on platform)
# For PyTorch: see CROSS_PLATFORM.md for platform-specific installation
# torch>=2.0.0  # Optional - see installation guide
# deepxde>=1.10.0  # Optional - requires TensorFlow or PyTorch backend
# torch-geometric>=2.4.0  # Optional - requires PyTorch
